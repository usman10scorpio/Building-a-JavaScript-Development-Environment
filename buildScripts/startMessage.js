// es5  var chalk = require('chalk');

// now we use babel so latest syntax in es6 is

import chalk from 'chalk';


console.log('------------------------------------');
console.log(chalk.green('starting ap in dev mode ....'));
console.log('------------------------------------');